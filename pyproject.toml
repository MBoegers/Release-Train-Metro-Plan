[project]
name = "release-train-metro-plan"
version = "0.1.0"
description = "OpenRewrite recipes and Jupyter notebooks for analyzing dependencies across repositories."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyterlab>=4.5.2",
    "notebook>=7.5.2",
    "pandas>=2.3.3",
    "papermill>=2.6.0",
]